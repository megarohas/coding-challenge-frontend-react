import * as React from "react";
import styled from "../../theme";

const HeaderWrapper = styled.div`
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 20px;
`;

class BeautyBtn extends React.PureComponent {
  render() {
    return <HeaderWrapper>evw</HeaderWrapper>;
  }
}

export default BeautyBtn;
